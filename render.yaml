services:
  - type: web
    name: spam-classifier
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "streamlit run spam_app.py --server.port=$PORT --server.address=0.0.0.0"
